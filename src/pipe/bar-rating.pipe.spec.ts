import { RateTitlePipe } from './rate-title.pipe';

describe('RateTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new RateTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
